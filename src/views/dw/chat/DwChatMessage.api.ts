import {defHttp} from '/@/utils/http/axios';
import { useMessage } from "/@/hooks/web/useMessage";

const { createConfirm } = useMessage();

enum Api {
  list = '/dw/chat/dwChatMessage/list',
  save='/dw/chat/dwChatMessage/add',
  edit='/dw/chat/dwChatMessage/edit',
  deleteOne = '/dw/chat/dwChatMessage/delete',
  deleteBatch = '/dw/chat/dwChatMessage/deleteBatch',
  importExcel = '/dw/chat/dwChatMessage/importExcel',
  exportXls = '/dw/chat/dwChatMessage/exportXls',
  history = '/dw/chat/dwChatMessage/history',
}
/**
 * 导出api
 * @param params
 */
export const getExportUrl = Api.exportXls;
/**
 * 导入api
 */
export const getImportUrl = Api.importExcel;
/**
 * 列表接口
 * @param params
 */
export const list = (params) =>
  defHttp.get({url: Api.list, params});

/**
 * 获取历史消息
 * @param sessionId 会话ID
 */
export const getHistory = (sessionId: string) =>
  defHttp.get({url: Api.history, params: { sessionId }});

/**
 * 删除单个
 */
export const deleteOne = (params,handleSuccess) => {
  return defHttp.delete({url: Api.deleteOne, params}, {joinParamsToUrl: true}).then(() => {
    handleSuccess();
  });
}
/**
 * 批量删除
 * @param params
 */
export const batchDelete = (params, handleSuccess) => {
  createConfirm({
    iconType: 'warning',
    title: '确认删除',
    content: '是否删除选中数据',
    okText: '确认',
    cancelText: '取消',
    onOk: () => {
      return defHttp.delete({url: Api.deleteBatch, data: params}, {joinParamsToUrl: true}).then(() => {
        handleSuccess();
      });
    }
  });
}
/**
 * 保存或者更新
 * @param params
 */
export const saveOrUpdate = (params, isUpdate) => {
  let url = isUpdate ? Api.edit : Api.save;
  return defHttp.post({url: url, params});
}
